%li{ :id => "item-#{group.id}"}
  %em
  %strong= link_to group.name, edit_admin_group_url(group)

  .more
    %span!= "#{t('.updated_at')} #{ l group.updated_at, :format => :short}"
    = link_to image_tag('admin/list/icons/trash.png'), admin_group_path(group._id), :class => 'remove', :confirm => t('admin.groups.index.confirm_group'), :method => :delete
