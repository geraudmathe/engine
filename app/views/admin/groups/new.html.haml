- title t('.title')


%p!= t('.help')

= semantic_form_for @group, :url => admin_groups_url do |form|
  = render "form", :f=>form
  
  
  = render 'admin/shared/form_actions', :back_url => admin_groups_url, :button_label => :create